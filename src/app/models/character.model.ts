import { defaultModel } from "./location-model";

export class characterModel {
    created: string;
    episode: Array<string>;
    gender: string;
    id: number;
    image: string;
    location: defaultModel;
    name: string;
    origin: defaultModel;
    species: string;
    status: string;
    type: string;
    url: string;
    isFavorite: boolean;
}